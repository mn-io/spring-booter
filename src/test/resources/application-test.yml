spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/springbooter_test
    username: root
    password: password

  liquibase:
    change-log: classpath:liquibase/changelog-dev.xml
    drop-first: true
